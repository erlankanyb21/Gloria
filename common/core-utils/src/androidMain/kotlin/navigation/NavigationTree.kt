package navigation

object NavigationTree {
    enum class SplashScreen{
        SplashScreen
    }

    enum class Auth{
//        AuthFlow,SignIn,
        SignUp, NumberConfirmation
    }

    enum class Main{
        MainScreen, Tab
    }
}